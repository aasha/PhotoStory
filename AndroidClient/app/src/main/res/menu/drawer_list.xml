<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/dr_feedback"
            android:checked="true"
            android:icon="@drawable/ic_feedback"
            android:title="Feedback" />
        <item
            android:id="@+id/dr_invite"
            android:icon="@drawable/ic_invite"
            android:title="Invite" />
        <item
            android:id="@+id/dr_contributor"
            android:icon="@drawable/ic_user"
            android:title="Become a contributor" />
        <item
            android:id="@+id/dr_wallpaper"
            android:icon="@drawable/icon_wallpaper"
            android:title="Set as wallpaper" />
    </group>

</menu>